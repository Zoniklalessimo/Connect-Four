class Team:
	def __init__(self, color, team_size):
		self.color = color
		self.team_size = team_size
		self.chips = list(list())

	def add_chip(x, y):
		chips.append([x, y])
		if test_win():
			return True
		else:
			return False
	
	def test_win():
		
